import React, { Component } from 'react';

// es6 class syntax
class MenuBusReset extends Component {
  render() {
    return (
      <div>
        <button>Clear Route</button>
      </div>
    )
  }
}

// app has access to state
export default MenuBusReset;
